import React from 'react';
import "../../styles/components/Header/title.scss"

const Title = () => {
    return (
        <div>
            <div className = "titleContainer">
                <div className = "titleImg"></div>
                <h1 className = "titleText">Chez vous, partout ou ailleurs</h1>
            </div>
        </div>
    );
};

export default Title;